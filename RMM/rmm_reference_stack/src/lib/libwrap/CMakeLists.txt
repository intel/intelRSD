SET(TARGET_LIB rmm_wrap)

#SET(SRC_LIB wrap.c am_api.c)

AUX_SOURCE_DIRECTORY(. SRC_LIB)

SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)

LINK_DIRECTORIES(${PROJECT_BINARY_DIR}/lib)

INCLUDE_DIRECTORIES(${PROJECT_BINARY_DIR}/include)

INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src/include)

ADD_LIBRARY(${TARGET_LIB} SHARED ${SRC_LIB})

TARGET_LINK_LIBRARIES(${TARGET_LIB} librmm_jsonrpcapi.so librmm_cfg.so librmm_utils.a libpthread.so -lm)
